@usedefaults

{{agentcontroller2job: jobid:$$jobid }}

h2. Job Details

{% for result in results %}

    | *Grid ID* | [${result.gid}] |
    | *Node ID* | [${result.nid}|/grid/node?id=${result.nid}&gid=${result.gid}] |
    | *Roles* | ${result.roles} | 
    | *Start Time* | ${result.starttime}|
    | *Time* | ${result.time} |
    | *Command* | ${result.cmd} |
    | *State* | ${result.state} |
    | *Data* | ${result.data} |


    h4. Args:
    {% for key, value in result.args.items() -%}
        |${key}|${value}|
    {% endfor %}

    h4. Messages:
    {% for msg in result.msgs -%}
        |${msg.level} | ${msg.data}|
    {% endfor %}


{% endfor %}