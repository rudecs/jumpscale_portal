@usedefaults

h5. JumpScript Details
{{jumpscript: id:$$id}}

{% if jumpscript %}
@@title JumpScript: ${name}

{{actions: 
- display: Execute Jumpscript
  showresponse: true
  action: '/restmachine/system/agentcontroller/executeJumpscriptUi'
  hide: false
  data:
   gid: ${jumpscript.Gid}
   name: ${jumpscript.Name}
   organization: ${jumpscript.Organization}
  input:
  - name: nid
    label: NodeId 
    placeholder: leave empty if you want to execute on roles
    type: number
  - name: roles
    label: role 
    placeholder: leave empty if you want to execute on nid
    type: number
{% for arg in args -%}
{% if arg %}
  - name: arg_"${arg}"
    label: "${arg}"
    type: text
{% endif %}
{% endfor -%}
 }}

||Property||Value||
{% for k, v in jumpscript.iteritems() -%}
	|${k} | ${v}|
{% endfor %}

{{code:
${source}
}}

h5. Jobs that have executed this Jumpscript
{{jobs: organization:$$organization jsname:$$jsname}}

{% else %}
Invalid jumpscript parameters
{% endif %}
